"""
Object are the instance of the class, every object has it own block of memory.

obj is the variable which hold the class instance on particular memory location.
by default obj variable are passed to the class

Ex.
    obj = A(obj)

"""


class A:
    pass


obj = A()

print(obj)
